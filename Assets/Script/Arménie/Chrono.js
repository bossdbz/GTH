#pragma strict


/*var chrono : GUIText;
private var textfield : String;
private var declencher : boolean;
 
function Start () {

	if ( declencher ){
		
	
	}

}

function Update () {

}

function StartChrono( time : int )
{
	
	// retrieve the GUIText Component and set the text
	chrono.enabled = true;
	textfield = chrono.GetComponent(GUIText);
	UpdateTimerText();
	// start the timer ticking
	TimerTick();
}

function UpdateTimerText()
{
	// update the textfield
	chrono.text = "time:" + currentTime.ToString();
}

function TimerTick( time : int )
{
	// while there are seconds left
	while(time > 0)
	{
		// wait for 1 second
		yield WaitForSeconds(1);
		// reduce the time
		currentTime--;
		UpdateTimerText();
	}
	
	// game over and restart
	Application.LoadLevel("Intro");
}*/